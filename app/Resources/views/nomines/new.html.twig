{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nomine creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}


    {#
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_start(delete_form, {'attr': {'onclick': 'return confirm("Are you sure?")'}}) }}
    <input type="submit" value="Delete" />
    {{ form_end(delete_form) }}
    {{ form_end(form) }}
#}

    <ul>
        <li>
            <a href="{{ path('nomines_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
